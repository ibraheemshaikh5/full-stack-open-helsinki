```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Once user clicks save preventDefault() stops default form submit with page reload and the new note is pushed to the list array

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    send a note in
    server-->>browser: 201 created
    Note right of browser: response is: {"message":"note created"}, there are no more additional updates or redirects
```